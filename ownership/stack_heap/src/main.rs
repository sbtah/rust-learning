fn main() {
    // Stack is fast but accepts smaller data types, of fixed size and this size has to be known at compile time.
    // Stack stores values in the sequential order it receives them.
    // Stack is LAST IN - FIRST OUT (LIFO) - Last plate on the stack is taken first - analogy.
    // PUSHING ONTO THE STACK!
    // POPPING OFF THE STACK!
    // Types that lives on the stack: Floats, Booleans, Chars, Arrays

    // The heap is generally slower but accepts bigger data, and allows for dynamic allocation over program's lifetime.
    println!("Hello, world!");
}
